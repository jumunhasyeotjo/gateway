server:
  port: 8080

spring:
  config:
    import: optional:file:./.env[.properties]
  application:
    name: gateway

jwt:
  secret: ${JWT_KEY}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER}